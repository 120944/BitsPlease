import application.BitsPleaseApp;

/**
 * Serves as an entry point for the app (not really needed as the main could be in BitsPleaseApp, might delete this if
 * it proves to be completely useless).
 */

public class EntryPoint {
    public static void main(String[] args){
        new BitsPleaseApp().Launch(args);
    }
}
